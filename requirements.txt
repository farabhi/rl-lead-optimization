# Exact versions from the environment that produced the results in the README.
#
# These are pinned rather than floated on purpose. This project's central
# finding is that a change in a NumPy sort implementation silently altered
# which compounds entered the seed pool, and that a change in the numerical
# environment sends every learned policy down a different trajectory. A
# floating dependency set would reintroduce exactly that.
#
# Recorded on: Python 3.13.15, AMD EPYC 7B12 (no AVX-512).
# The notebook prints a full environment fingerprint on every run; compare
# it against yours before trusting any cross-run comparison.

numpy==2.1.3
pandas==2.2.3
scipy==1.16.3
scikit-learn==1.6.1
rdkit==2026.3.5
matplotlib==3.10.0
chembl-webresource-client==0.10.9
pyarrow            # parquet engine for the cached dataset
